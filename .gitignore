# Dependencies
node_modules/
package-lock.json
yarn.lock

# Build output
dist/
build/
*.log

# IDE
.vscode/
!.vscode/tasks.json
.idea/
*.swp
*.swo
*~

# OS
.DS_Store
Thumbs.db

# Python
__pycache__/
*.pyc
*.pyo

# Test coverage
coverage/
.nyc_output/

# Extension packaging
*.zip
*.crx
*.pem

# Environment
.env
.env.local

# AI evaluation output (local only — contains live ADO data)
.ado-eval/

# Temporary files
tmp/
temp/

# Playwright CLI config
.playwright-cli/

# ADO eval output (generated, not committed)
.ado-eval/

# AI toolkit
.aikit/

# SmartClient runtime: we bundle a ~27MB LGPL subset (modules + 5 skins)
# directly in the repo so the dashboard works out of the box on fresh clones.
# Full SDK is available from smartclient.com for users who want more skins.

# Plugin assembly outputs (extension/apps/<id>/) — private plugins are
# assembled from outside repos at build time and never enter public history.
# Checked-in exceptions:
#   - apps/README.md       — explains the convention
#   - apps/index.json      — plugin discovery list
#   - apps/hello-runtime/  — in-tree reference plugin
#   - apps/horsebread/PLAN.md (legacy placeholder)
extension/apps/*
!extension/apps/README.md
!extension/apps/index.json
!extension/apps/_loaded.js
!extension/apps/hello-runtime/
!extension/apps/sqlite-query/
!extension/apps/csv-analyzer/
!extension/apps/pf-text-converter/
!extension/apps/horsebread/
extension/apps/horsebread/*
!extension/apps/horsebread/PLAN.md

# Screenshots & data files
Capture.PNG
twinspires-races.txt
*.png
nul
.specstory/
.npmrc

# Wiki checkout (synced via scripts/sync-wiki.mjs)
.wiki/

# Zato SQLite backups (binary, created by backup command)
docker/zato/sql/petstore-backup.db
