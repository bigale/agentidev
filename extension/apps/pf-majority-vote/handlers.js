/**
 * pf-majority-vote plugin handlers — generated from cookbook/pocketflow-majority-vote.
 *
 * Idempotent FLOW_DEFINE on each run keeps the plugin file as the
 * source of truth. Form values flow into the Python flow via shared state.
 *
 * Generated by scripts/cookbook-to-plugin.mjs. Hand-edit as needed.
 */

const FLOW_SOURCE = "\"\"\"\npf-majority-vote — generated from cookbook/pocketflow-majority-vote\n\nGenerated by scripts/cookbook-to-plugin.mjs. Edit the plugin's handlers.js\n(FLOW_SOURCE constant) to customize. Each click on the plugin re-saves\nthe flow to ~/.agentidev/flows/pf-majority-vote.py.\n\"\"\"\n# === utils.py ===\n# utils.py replaced by agentidev_llm (bridge /llm endpoint)\n\n\n# === main.py ===\nimport argparse\nfrom pocketflow import BatchNode, Flow\nimport collections\nfrom agentidev_llm import call_llm\nimport yaml\n\nclass MajorityVoteNode(BatchNode):\n    def prep(self, shared):\n        question = shared.get(\"question\", \"(No question provided)\")\n        attempts_count = shared.get(\"num_tries\", 3)\n        return [question for _ in range(attempts_count)]\n\n    def exec(self, single_question: str):\n        prompt = f\"\"\"\nYou are a helpful assistant. Please answer the user's question below.\nQuestion: {single_question}\n\nReturn strictly using the following YAML structure:\n```yaml\nthinking: |\n    (Your thinking process here)\nanswer: 0.123 # Final answer as a decimal with 3 decimal places\n```\"\"\"\n        raw_response = call_llm(prompt)\n        yaml_part = raw_response.split(\"```yaml\")[1].split(\"```\")[0].strip()\n        parsed = yaml.safe_load(yaml_part)\n\n        # Validate we have at least 'answer' field\n        if not isinstance(parsed, dict) or 'answer' not in parsed:\n            raise RuntimeError(f\"Missing 'answer' in YAML: {parsed}\")\n\n        # Return only the 'answer' field for the majority vote.\n        return str(parsed['answer'])\n    \n    def exec_fallback(self, prep_res, exc):\n        return None\n\n    def post(self, shared, prep_res, exec_res_list):\n        # Count frequency for non-None answers\n        exec_res_list = [res for res in exec_res_list if res is not None]\n        counter = collections.Counter(exec_res_list)\n        best_answer, freq = counter.most_common(1)[0]\n\n        # Store final\n        shared[\"majority_answer\"] = best_answer\n\n        print(\"========================\")\n        print(\"All structured answers:\", exec_res_list)\n        print(\"Majority vote =>\", best_answer)\n        print(\"Frequency =>\", freq)\n        print(\"========================\")\n\n        # End the flow\n        return \"end\"\n\n\n# WARNING: no module-scope `Flow(...)` or `def main()` found — add a stdin/stdout shim manually.\n";

const FLOW_NAME = "pf-majority-vote";
const PLUGIN_ID = "pf-majority-vote";

export function register(handlers) {
  handlers["PF_MAJORITY_VOTE"] = async (msg) => {
    const defineRes = await handlers['FLOW_DEFINE']({ name: FLOW_NAME, source: FLOW_SOURCE });
    if (!defineRes.success) {
      return { success: false, error: 'flow define failed: ' + (defineRes.error || 'unknown') };
    }
    // Pass the form values as the initial shared state. Field names should
    // match the keys the flow's prep() reads from shared.
    const shared = Object.assign({}, msg || {});
    delete shared.type; // dispatch metadata, not for the flow
    const runRes = await handlers['FLOW_RUN']({
      name: FLOW_NAME,
      pluginId: PLUGIN_ID,
      shared,
      timeout: 90000,
    });
    if (!runRes.success) {
      return { success: false, error: 'flow run failed: ' + (runRes.error || 'unknown'), stderr: runRes.stderr };
    }
    const final = runRes.shared || {};
    return { success: !final.error, shared: final, error: final.error || null };
  };
}
