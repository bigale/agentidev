#!/usr/bin/env node
/**
 * Auto-generated API test: DELETE /pet/{petId}
 * Generated by api-to-app pipeline from OpenAPI spec.
 * PICT rows: 25 (pairwise combinatorial coverage)
 */
import { ScriptClient } from '../packages/bridge/script-client.mjs';

const BASE_URL = 'https://petstore.swagger.io/v2';
const client = new ScriptClient('test-petstore-deletePet', { totalSteps: 25 });

const cases = [
  {
    "api_key": "doggie",
    "petId": "1",
    "Accept": "application_json",
    "Auth": "valid_auth",
    "_negative": false
  },
  {
    "api_key": "doggie",
    "petId": "100",
    "Accept": "application_json",
    "Auth": "valid_auth",
    "_negative": false
  },
  {
    "api_key": "cat_42",
    "petId": "1",
    "Accept": "application_json",
    "Auth": "valid_auth",
    "_negative": false
  },
  {
    "api_key": "cat_42",
    "petId": "9999",
    "Accept": "application_json",
    "Auth": "valid_auth",
    "_negative": false
  },
  {
    "api_key": "doggie",
    "petId": "9999",
    "Accept": "application_json",
    "Auth": "valid_auth",
    "_negative": false
  },
  {
    "api_key": "cat_42",
    "petId": "100",
    "Accept": "application_json",
    "Auth": "valid_auth",
    "_negative": false
  },
  {
    "api_key": "~empty_string",
    "petId": "100",
    "Accept": "application_json",
    "Auth": "valid_auth",
    "_negative": true
  },
  {
    "api_key": "doggie",
    "petId": "1",
    "Accept": "application_json",
    "Auth": "~invalid_auth",
    "_negative": true
  },
  {
    "api_key": "cat_42",
    "petId": "1",
    "Accept": "application_json",
    "Auth": "~no_auth",
    "_negative": true
  },
  {
    "api_key": "doggie",
    "petId": "~abc",
    "Accept": "application_json",
    "Auth": "valid_auth",
    "_negative": true
  },
  {
    "api_key": "~empty_string",
    "petId": "1",
    "Accept": "application_json",
    "Auth": "valid_auth",
    "_negative": true
  },
  {
    "api_key": "cat_42",
    "petId": "~-1",
    "Accept": "application_json",
    "Auth": "valid_auth",
    "_negative": true
  },
  {
    "api_key": "doggie",
    "petId": "9999",
    "Accept": "application_json",
    "Auth": "~no_auth",
    "_negative": true
  },
  {
    "api_key": "cat_42",
    "petId": "~abc",
    "Accept": "application_json",
    "Auth": "valid_auth",
    "_negative": true
  },
  {
    "api_key": "~null",
    "petId": "1",
    "Accept": "application_json",
    "Auth": "valid_auth",
    "_negative": true
  },
  {
    "api_key": "doggie",
    "petId": "~-1",
    "Accept": "application_json",
    "Auth": "valid_auth",
    "_negative": true
  },
  {
    "api_key": "~empty_string",
    "petId": "9999",
    "Accept": "application_json",
    "Auth": "valid_auth",
    "_negative": true
  },
  {
    "api_key": "~null",
    "petId": "9999",
    "Accept": "application_json",
    "Auth": "valid_auth",
    "_negative": true
  },
  {
    "api_key": "cat_42",
    "petId": "100",
    "Accept": "application_json",
    "Auth": "~invalid_auth",
    "_negative": true
  },
  {
    "api_key": "cat_42",
    "petId": "9999",
    "Accept": "~text_plain",
    "Auth": "valid_auth",
    "_negative": true
  },
  {
    "api_key": "doggie",
    "petId": "9999",
    "Accept": "application_json",
    "Auth": "~invalid_auth",
    "_negative": true
  },
  {
    "api_key": "doggie",
    "petId": "100",
    "Accept": "application_json",
    "Auth": "~no_auth",
    "_negative": true
  },
  {
    "api_key": "doggie",
    "petId": "1",
    "Accept": "~text_plain",
    "Auth": "valid_auth",
    "_negative": true
  },
  {
    "api_key": "doggie",
    "petId": "100",
    "Accept": "~text_plain",
    "Auth": "valid_auth",
    "_negative": true
  },
  {
    "api_key": "~null",
    "petId": "100",
    "Accept": "application_json",
    "Auth": "valid_auth",
    "_negative": true
  }
];

function stripNeg(v) {
  return typeof v === 'string' && v.startsWith('~') ? v.slice(1) : v;
}


function buildPath(c) {
  return `/pet/${stripNeg(c.petId)}`;
}


try {
  await client.connect();
  console.log('DELETE /pet/{petId} — ' + cases.length + ' test cases');
  console.log('==================================================\n');


  for (let i = 0; i < cases.length; i++) {
    const c = cases[i];
    const caseLabel = Object.entries(c).filter(([k]) => !k.startsWith('_')).map(([k, v]) => k + '=' + v).join(', ');
    await client.progress(i + 1, cases.length, 'Case ' + (i + 1));

    try {
      const url = BASE_URL + buildPath(c);
      const headers = {};
      headers['Accept'] = stripNeg(c.Accept).replace(/_/g, '/');
      if (stripNeg(c.Auth) === 'valid_auth') headers['Authorization'] = 'Bearer test-token';
      else if (stripNeg(c.Auth) === 'invalid_auth') headers['Authorization'] = 'Bearer invalid';
      if (stripNeg(c.api_key) !== 'empty_string' && stripNeg(c.api_key) !== 'null') headers['api_key'] = stripNeg(c.api_key);
      const fetchOpts = { method: 'DELETE', headers };
      const resp = await fetch(url, fetchOpts);

      if (c._negative) {
        // Negative case: accept any response (server may be lenient)
        client.assert(true, 'Case ' + (i+1) + ': negative handled (got ' + resp.status + ')');
        if (resp.status >= 200 && resp.status < 300) {
          console.log('  Case ' + (i+1) + ': server lenient on negative input');
        }
      } else {
        // Positive case: expect success. 404 is acceptable for GET/DELETE with
        // IDs on a shared server (stateful dependency — the resource may not exist).
        const ok2xx = resp.status >= 200 && resp.status < 300;
        const statefulOk = (resp.status === 404) && ('DELETE' !== 'POST');
        client.assert(ok2xx || statefulOk,
          'Case ' + (i+1) + ': returns ' + resp.status + (statefulOk ? ' (404 ok — stateful)' : ''));


      }
    } catch (err) {
      client.assert(false, 'Case ' + (i+1) + ': ' + err.message);
    }
  }


  console.log('\n');
  const exitCode = client.summarize();
  await client.complete({ assertions: client.getAssertionSummary() });
  process.exit(exitCode);

} catch (err) {
  console.error('\nFatal:', err.message);
  client.assert(false, 'Fatal: ' + err.message);
  client.summarize();
  await client.complete({ assertions: client.getAssertionSummary() }).catch(() => {});
  process.exit(1);
}
